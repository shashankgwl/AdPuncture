{
  "name": "adpuncture",
  "version": "1.0.0",
  "private": true,
  "description": "Dependency-free, locally loadable Chrome seek controls",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "fixture": "node tests/serve.cjs"
  },
  "engines": { "node": ">=18" }
}